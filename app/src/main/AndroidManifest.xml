<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edecora">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EDecora">
        <activity android:name=".CheckOut"></activity>
        <activity android:name=".ScrollApp" />
        <activity android:name=".OtherMain" />
        <activity android:name=".FestivalMain" />
        <activity android:name=".WeddingMain" />
        <activity android:name=".AnniversaryMain" />
        <activity android:name=".EventMain" />
        <activity android:name=".Cart" />
        <activity android:name=".BirthdayMain" />
        <activity
            android:name=".MyProfile"
            android:label="@string/title_activity_my_profile" />
        <activity android:name=".MyInformation" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>